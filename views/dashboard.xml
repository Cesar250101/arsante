<odoo>
  <data>

    <!-- Client Action for OWL Dashboard -->
    <record id="action_tipo_registro_dashboard" model="ir.actions.client">
        <field name="name">Dashboard Tipo Registro</field>
        <field name="tag">arsante_tipo_registro_dashboard</field>
    </record>

    <!-- Update existing menu or create it if not exists -->
    <menuitem name="DashBoard" 
              id="arsante.dashboard_cda_cosmetico_dm_menu_list" 
              parent="arsante.arsante_menu_root"              
              action="action_tipo_registro_dashboard"
              sequence="1"/>
   
  </data>
</odoo>